name: run

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  schedule:
    - cron: '10 2 * * *'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          
      - name: Create package.json with dependencies
        run: |
          cat > package.json << 'EOF'
          {
            "name": "glados-checkin",
            "version": "1.0.0",
            "type": "module",
            "dependencies": {
              "node-fetch": "^3.3.0"
            }
          }
          EOF
          npm install
          
      - name: Create and run checkin script
        run: |
          cat > checkin.js << 'EOF'
          // 这里粘贴上面完整的修复代码
          // 从 console.log('=== 开始执行 GLaDOS 签到 ==='); 开始
          // 到 main(); 结束
          EOF
          
          echo "📄 脚本内容预览（前10行）："
          head -10 checkin.js
          
          echo "🚀 开始执行脚本..."
          node checkin.js
        env:
          GLADOS: ${{ secrets.GLADOS }}
          NOTIFY: ${{ secrets.NOTIFY }}
